{
  "name": "functions",
  "description": "Cloud Functions for CREART - Full API",
  "scripts": {
    "serve": "echo Use npm run dev in project root for frontend only",
    "dev": "echo Starting frontend only (Firebase real)... && cd .. && npm run dev",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "engines": {
    "node": "20"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^6.0.0",
    "mercadopago": "^2.0.0"
  }
}
